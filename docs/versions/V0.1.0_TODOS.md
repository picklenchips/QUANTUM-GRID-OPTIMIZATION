# V0.1.0 TODOS — priority-ordered

> Merges [V0_SUMMARY.md](V0_SUMMARY.md)'s code-state punch list (still open, unfixed) with this pass's research-driven recommendations. Highest-leverage first.

**Done, 2026-08-12** (ralph-loop V0.0.1, see [docs/pipeline/state.md](../pipeline/state.md)): `pp_to_microgrid.py` syntax error + downstream bugs fixed; HIFLD → Microsoft GridSFM switch; `network_from_OSM()` / `raw_to_pp()` stubs finished.

**Also done, same day** (quick fix-everything pass, 2 commits: 56b20d40, c5e2c140): hardcoded Mapbox tokens removed from code (env var instead) — dashboard-side rotation still needed, see item 1 below; `pp_to_microgrid.py`'s modularity/electrical-coupling/sensitivity path (previously documented-but-unfixed) now genuinely fixed and verified; `data/ppnets/transnet-california-n.db` regenerated under current pandapower, the 3.0-breaking-changes incompatibility is actually resolved now, not just flagged; `environment.yml`'s `ortools` → `ortools-python` and D-Wave SDK pin; `quantum_util.py`'s `PLOTDIR` import fixed; QPGrid app dead nav buttons + broken image path fixed. `pp_learn.py` deliberately left as-is (needs a real rewrite, not a patch) with a status note explaining why.

**Done, 2026-08-27** (tooling-integration pass, see [V0.2.0_SUMMARY.md](V0.2.0_SUMMARY.md) + [../08-TOOLING-INTEGRATION.md](../08-TOOLING-INTEGRATION.md)): every tool in 07-TOOLING-UPDATES.md now has a Python hook — `data/opfdata_to_pp.py`, `data/pglearn_to_pp.py`, `data/gridfm_datakit_hook.py`, `data/openinframap_hook.py`, `data/hifld_archive.py`, `data/climrr_overlay.py`, `pypsa_bridge.py`. `environment.yml` rebuilt on python 3.12 (working `qpgrid` conda env, pandapower 3.5.4 + pypsa 1.3 + dwave-ocean 9.4 + gridfm-datakit). `notebooks/tooling_integration.ipynb` runs all 8 offline (nbconvert-clean); `web/tooling_dashboard.html` renders the status matrix + a converted network on an OpenInfraMap basemap. Also fixed `data/osm_to_pp.py`'s `net['bus_geodata']` (pandapower-3.0-removed table) — the `network_from_OSM()` finish from f7503c44 was never run under pandapower 3.x. Items 3, 4, 5 below are now unblocked (the baselines/tools they name are wired in).

1. **Rotate the Mapbox tokens in the Mapbox dashboard itself** — code no longer hardcodes them (reads `MAPBOX_TOKEN` env var now), but the actual token values were exposed in git history on a public repo and are still live/valid until rotated there. This step needs dashboard access I don't have.
2. **Read [arXiv:2406.05916](https://arxiv.org/abs/2406.05916)** before re-touching the QUBO formulation — compact/lossless upgrade to the same modularity+self-reliance lineage already implemented.
3. **Upgrade the classical baseline `pp_to_microgrid.py` competes against**: swap or supplement `SimulatedAnnealingSampler` (CPU) with a GPU quantum-inspired solver (Simulated Bifurcation-style, or benchmark against NVIDIA cuOpt for the MILP-formulated version). Per [05-QUANTUM-UPDATES.md](../05-QUANTUM-UPDATES.md) §2, this is now the real bar, not plain CPU SA.
4. **Decide on ACOPF baseline to actually try to beat**: read [Revisiting Deep AC-OPF](https://arxiv.org/abs/2509.00655) (linear regression beats several DL claims) and [GridSFM (Microsoft)](https://www.microsoft.com/en-us/research/blog/gridsfm-a-new-small-foundation-model-for-the-electric-grid/) (1,000× speedup, ~2-3% cost gap) before committing to a quantum ACOPF approach — [06-ML-SOTA.md](../06-ML-SOTA.md) has the full two-tier bar.
5. Consider PyPSA v1.0 if/when this project wants uncertainty-aware optimization (stochastic demand/renewable variability) — capability pandapower doesn't have natively.
6. Read the two most on-topic finds from this pass in full before further quantum design work: [IonQ+ORNL Unit Commitment](https://www.ionq.com/news/ionq-partners-with-oak-ridge-national-laboratory-demonstrating-quantum-power) and the new anchor survey [Quantum computing for smart grid](https://www.nature.com/articles/s44287-026-00295-6) (*Nat Rev Elec Eng*, May 2026).
7. `pp_learn.py` needs a real rewrite (undefined/private pandapower internals) if it's ever going to be used — currently just left with an honest status-note comment, not fixed.

---

*Related: [[V0.1.0_SUMMARY]] · [[V0_SUMMARY]] · [[../05-QUANTUM-UPDATES]] · [[../06-ML-SOTA]] · [[../07-TOOLING-UPDATES]]*
